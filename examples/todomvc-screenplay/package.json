{
  "name": "todomvc-screenplay",
  "version": "0.0.0",
  "description": "Screenplay Pattern implementation for the TodoMVC app",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "setup": "npm run typings -- install",
    "lint": "tslint --config ../../tslint.json --project ./tsconfig.json --format stylish",
    "clean": "rimraf lib",
    "transpile": "tsc",
    "build": "npm run clean && npm run lint && npm run transpile",
    "prepublish": "npm run setup && npm run build",
    "test": "exit 0;",
    "typings": "typings"
  },
  "author": "Jan Molak <jan.molak@smartcodeltd.co.uk>",
  "license": "Apache-2.0",
  "dependencies": {
    "serenity-bdd": "file:../../"
  },
  "devDependencies": {
    "rimraf": "2.5.4",
    "tslint": "3.14.0",
    "typescript": "1.8.10",
    "typings": "^1.3.2"
  }
}
