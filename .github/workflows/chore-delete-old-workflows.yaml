name: "Chore: Delete old workflows"
on:
  schedule:
    # Run daily, at 19:14
    - cron: '14 19 * * *'

jobs:
  delete_old_workflows:
    runs-on: ubuntu-latest
    steps:
      - name: Delete old workflows
        uses: Mattraks/delete-workflow-runs@708edabfaf5f9b6357ce4c375d22c83834c87f7d # v2
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 30
          keep_minimum_runs: 5
#          dry_run: 'true'
