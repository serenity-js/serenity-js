export = function() {

    this.Given(/^Ambigail logs in as (.*@.*)$/, email => {}); // tslint:disable-line:no-empty

    this.Given(/^Ambigail logs in as (.*)/,  username => {}); // tslint:disable-line:no-empty
};
