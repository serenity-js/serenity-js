describe('Mocha', () => {

    describe('A scenario', () => {

        it('passes the first time', () => {
            // no-op
        });

        it('passes the second time', () => {
            // no-op
        });

        it('passes the third time', () => {
            // no-op
        });
    });
});
