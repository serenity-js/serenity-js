# System Context

The System Context view displays the test environment: Node.js version, test runner, operating system, browser versions, and CI/CD information (provider, build number, branch, commit). This helps correlate test results with infrastructure.
