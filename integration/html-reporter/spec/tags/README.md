# Tags

The Tags view aggregates test results by tag (features, browsers, custom tags). It shows pass rates per tag and enables navigation to the filtered scenario list for any tag — useful for assessing feature-area health.
