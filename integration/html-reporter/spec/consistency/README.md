# Consistency

The Consistency view identifies tests whose outcomes vary across runs — flaky tests that pass sometimes and fail others. It distinguishes between degraded tests (newly failing), recovered tests (newly passing), and intermittently inconsistent tests.
