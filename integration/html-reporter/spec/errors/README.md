# Errors

The Errors view groups failing scenarios by error type and message, helping identify systemic issues. It surfaces whether failures cluster around specific error patterns (e.g., all timeouts, all in one module) or are diverse independent issues.
