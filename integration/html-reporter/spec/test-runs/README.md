# Test Runs

The Test Runs view shows execution history as a trend chart and run list. Clicking a historical run navigates to the filtered scenario view for that point in time, enabling investigation of when regressions were introduced.
