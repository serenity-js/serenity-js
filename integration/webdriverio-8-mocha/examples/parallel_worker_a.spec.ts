import { describe, it } from 'mocha';

describe('Mocha', () => {

    describe('Worker A', () => {

        it('passes test A1', () => {
            // no-op
        });

        it('passes test A2', () => {
            // no-op
        });
    });
});
