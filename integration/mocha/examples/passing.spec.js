describe('Mocha reporting', () => {

    describe('A scenario', () => {

        afterEach(() => {
            // no-op, passing
        })

        it('passes', () => {
            // no-op, passing
        });
    });
});
